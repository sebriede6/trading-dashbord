# Docker Compose defaults
POSTGRES_USER=appuser
POSTGRES_PASSWORD=local-pass-change-me
POSTGRES_DB=appdb

# Optional: override container-internal database host/port for the backend service
DB_HOST=postgres
DB_PORT=5432
DB_USER=appuser
DB_PASSWORD=local-pass-change-me
DB_NAME=appdb
